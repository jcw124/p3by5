import React from "react";

const Edit = () => (
    <div className="container">
        <button 
            //onClick={}
            className="newGame btn btn-primary">
                New Game
        </button>

    </div>
);

export default Edit;